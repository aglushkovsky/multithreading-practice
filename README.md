задача 1 по `CompletableFuture`:

создать метод c возвращаемым типом `CompletableFuture<CacheObject>`, который бы возвращал объект л из кеша по его id, а если в кеше нет, то пойти "во внешнюю систему". Также предусмотреть обработку ошибок внутри этого метода. `CacheObject` сам создашь с 3 полями (один из них id), создашь коллекцию для кеша и метод для имитации похода во внешнюю систему (который с рандомной задержкой бы отдавал сгенерированный `CacheObject` или отдавал ошибку)